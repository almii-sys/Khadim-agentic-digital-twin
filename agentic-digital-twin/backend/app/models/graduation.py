"""
graduation.py — application-level logic for the [FILL IN MODULE NAME] module.

Import shared schema from app.mock_data.models / app.mock_data.unitime_models
as needed rather than redefining tables here. This file is for
module-specific business logic, service functions, and API route handlers
that sit on top of the shared mock data layer.

Owner: <assign in docs/module-ownership.md>
"""
